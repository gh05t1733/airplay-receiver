# Studio Pipeline — Handoffs

| Date (ISO) | From | To | Artifact | Status |
|---|---|---|---|---|
| 2026-09-16 | architect | deployer, code-executor, reviewer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.6 — **root cause of the empty artifact, and a better fix than a glob.** `RUNTIME_OUTPUT_DIRECTORY` is set nowhere in the repo, so `add_subdirectory(src)` puts binaries in a per-target/per-config subdir (`build/src/Release/airplay-receiver.exe` with the VS generator). `path: build/Release/*.exe` therefore matched **nothing** — and now **two** runs concluded `success` (`35013172955`, `35013648139`) while the repo's artifact total is **0** (`gh api …/actions/artifacts --jq .total_count` → `0`, verified). Deployer's `build/**/Release/*.exe` (commit `4882a99`) fixes the VS case but breaks again under Ninja; the durable fix specified in §6.2 is to pin it in CMake once (`CMAKE_RUNTIME_OUTPUT_DIRECTORY = ${CMAKE_BINARY_DIR}/bin` + the `_<CONFIG>` form for multi-config) → upload path becomes `build/bin/*.exe` for every generator, and applocal DLLs land beside the exe for the Sprint 4 installer. Also corrected the gate 1.2/1.11 instructions: `_airplay._tcp` and `_raop._tcp` carry **different** TXT key sets, so requiring `md`/`ft`/`am`/`sf` on `_airplay._tcp` would make gate 1.11 impossible to pass. Brief v3.6: ~1075 lines |
| 2026-09-16 | architect | deployer, reviewer, code-executor | docs/architecture/ARCHITECTURE-BRIEF.md | V3.5 — **found the false green.** Run `35013172955` (commit `63b57c2`) concluded **`success`** in 12 m 1 s, but its own annotation says `No files were found with the provided path: build/Release/*.exe. No artifacts will be uploaded.` and `gh api …/runs/35013172955/artifacts` returns **empty**. So gate 1.1's artifact evidence does **not** exist for any run yet, and that green tick is exactly the "green but empty" class — it also proves rule 1 empirically (Ninja configure + multi-config artifact path = success + nothing). Actions: (a) new rule 4 in §6.3 — `upload-artifact@v4` MUST set `if-no-files-found: error`; (b) new rule 5 — presets must load (`$schema` ⇒ `"version": 8`, else `cmake --list-presets` exits 1 and looks like a configure failure; add it as a CI step); (c) new **§6.1.1 `third_party/` policy** — `third_party/mDNSResponder/include/dns_sd.h` **must be committed** (vendored Apache-2.0), ignore is the single scoped line `third_party/ffmpeg/`, never a blanket `third_party/` + `!` (git cannot re-include under an excluded parent — silently drops the header at clone time), with the mandatory two-sided `git check-ignore` test. Verified locally: `.gitignore` still has **zero** `third_party` rules and `third_party/` does not exist on disk yet. Brief v3.5: ~1050 lines |
| 2026-09-16 | architect | deployer, reviewer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.4 — **CI section corrected against the runner, twice.** My "CI = VS 17 2022" correction was **wrong**; the workflow's own `vswhere` step (runs 35007094126/35009325571/35011478579/35011872714, all *failure*) proves `windows-latest` = **VS 18 Enterprise 18.0 / MSVC 14.51.36231**, identical to the local toolset. Brief now mandates ONE generator (`Visual Studio 18 2026`) on both sides, keeps the `toolchain facts` step as a permanent rule ("print, don't infer"), and adds a `git -C $env:VCPKG_INSTALLATION_ROOT rev-parse HEAD` line so the real baseline hash is *printed* for a later pin (commit `c665ced` pinned `9e44ec0e…`, which the runner's `C:\vcpkg` does not contain → run 35011478579 died in 23 s). Also records three generalisable CI rules: (1) generator↔artifact-path coupling (a Ninja configure with a `build/Release/*.exe` upload = green build, empty artifact), (2) one dependency source of truth — classic-mode install from a temp dir bypasses `vcpkg.json` and hides the `mdns`/`video` features, (3) a missing `find_package` fails at *generate* (`spdlog::spdlog` bug, run 35011872714). Verified independently via `gh run list`: 8 commits on `main`, latest 6 runs = 4 failure / 2 in progress (35013172955, 35012963622). Brief v3.4: ~1015 lines |
| 2026-09-16 | architect | deployer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.3.1 — feature name aligned to the shipped manifest (`mdns`, not `mdns-vendored`), and the mdnsresponder port claim replaced with @deployer's **measurement**: `dnssd.dll`/`dnssd.lib` build fine, only the bundled `dns-sd` CLI fails (`RC1015 afxres.h`, no MFC in VS 18 BuildTools) → the port stays a viable fallback and the gate verifier is the **system** `dns-sd.exe`. Also confirmed on disk: `vcpkg.json` now has default deps `openssl, spdlog, catch2` + opt-in features `mdns`/`video`, and `assets/` (i18n en/id) is still **untracked** — needs `git add assets` before the first push |
| 2026-09-16 | architect | deployer, code-executor, reviewer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.3 — **mDNS redesigned around the running Bonjour daemon** (foundry finding, verified independently). Evidence on disk: `Bonjour Service` RUNNING (`mDNSResponder.exe` pid 10956) holding `192.168.1.14:5353` while three other PIDs hold `0.0.0.0:5353`; `C:\Windows\System32\dnssd.dll` present (85,864 B) and `dns-sd.exe` present (96,104 B); **no Bonjour SDK and no `dns_sd.h`** anywhere; **no `atlmfc`** in VS 18 BuildTools (so the SDK's `dns-sd` CLI and ATL/MFC-flavoured ports cannot build here). Decisions: (a) C1/advertiser registers **through the daemon** (`DNSServiceRegister` in `dnssd.dll`, dynamically loaded, vendored Apache-2.0 `dns_sd.h` from apple-oss-distributions) and **never binds 5353**; (b) vcpkg `mdnsresponder` moves behind the optional feature `mdns-vendored` → **Sprint 1 default deps become openssl + spdlog + catch2**, no FFmpeg, no ATL; (c) gate 1.2/1.11 verifier = system `dns-sd.exe`; gate 1.3 now requires proof our PID is **not** the 5353 owner; (d) Sprint 4 gate 4.3 + C19 gain the Bonjour runtime-dependency item (Apple redistribution terms to be checked before bundling); (e) R4 rewritten with this evidence + the port-conflict cause. Brief v3.3: ~985 lines |
| 2026-09-16 | architect | deployer, code-executor, reviewer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.2 — **FFmpeg decoupled from Sprint 1** (new gate 2.0). Verified on disk: no `find_package`/FFMPEG reference exists in the scaffold except `find_package(Catch2 3 CONFIG REQUIRED)` — nothing links libavcodec yet, so the FFmpeg source build is NOT a Sprint 1 requirement and must not gate gate 1.1. Decision: FFmpeg becomes the optional vcpkg manifest feature `video` (enabled with `-DVCPKG_MANIFEST_FEATURES=video`) behind CMake option `AIRPLAY_ENABLE_FFMPEG` (default OFF); Sprint 1 local+CI builds with media OFF, CI turns it on from Sprint 2. New **gate 2.0** = FFmpeg-dev linkable (vcpkg feature OR BtbN lgpl + `-DAIRPLAY_FFMPEG_ROOT=`) + a smoke decode of one canned H.264 access unit; Sprint 2 does not start without it. Retry policy written down: max 2 attempts with `--x-no-parallel-installs` and heavy apps closed, then plan B — the observed failure was FFmpeg's `configure` fork dying (`0xC0000142`, `fork: Resource temporarily unavailable`) at ~5.7 GB free of 15.8 GB RAM (confirmed: `wmic` reports 6,020,216 KB free). Risk register gained **R14** (advertised mask ≠ implemented pairing → dangling ref in gate 1.11 now resolved) and **R15** (local dependency source-build resource exhaustion). Brief v3.2: ~975 lines |
| 2026-09-16 | architect | deployer, reviewer | docs/architecture/ARCHITECTURE-BRIEF.md | V3.1 — (a) **signed off** the `vcpkg-configuration.json` deviation (builtin registry; brief §3/§6.1/§6.3 updated to match the file that will actually ship), (b) **new verified blocker found**: `VCPKG_ROOT` is unset (not in shell, not in `HKCU\Environment` — only `VCPKG_BINARY_SOURCES`/`VCPKG_DISABLE_METRICS` are) while `CMakePresets.json` resolves the local toolchain as `$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake` → every local preset configures with an empty toolchain path. Fix added to §6.4 (`setx VCPKG_ROOT C:\dev\vcpkg`); deployer should also hardcode `C:/dev/vcpkg` in the `local-*` presets rather than trust an env var. Verified on disk: commit `adaee6a`, branch `master`, 0 remote, `gh` absent, `vcpkg.json` = ffmpeg[avcodec,avformat,swscale,swresample,nvcodec] with `default-features:false` + mdnsresponder/openssl/spdlog/catch2 |
| 2026-09-16 | architect | prd-maker, ui-designer, deployer | docs/architecture/ARCHITECTURE-BRIEF.md | V3 — synchronized with the 4 new locked decisions + 2 CI guards. (a) `features` row now the locked transient mask `0x40000280,0x10400` (bits 7/9/30/42/48, bit 27 absent) — recomputed independently in python, matches PRD; (b) D5 transient + D6 English-default/i18n recorded in §9.1, §9.2 item 1 marked resolved; (c) C16 no longer promises a Sprint-2 PIN surface; (d) i18n contract added to §5.8 (`set_locale`, `locales`/`locale` in hello, `assets/i18n/<locale>.json`, fallback-to-en rule) + C13 `locale` + tree entry; (e) ci.yml: no-op `-DCMAKE_BUILD_TYPE` removed (multi-config), vcpkg binary cache + `VCPKG_DISABLE_METRICS`, build dir must stay in-repo (MSB8029); (f) `.gitignore` guard lines documented in the tree (vcpkg_installed/, vcpkg-manifest-install.log, CMakeUserPresets.json); (g) §8 now splits CI gates (compile+ctest, no GPU) from local GPU gates — perf bars can never be satisfied by CI. Brief v3: 952 lines / 71,801 bytes |
| 2026-09-16 | architect | prd-maker | docs/architecture/ARCHITECTURE-BRIEF.md | REVISED after reviewer gate — (a) CI generator fixed to "Visual Studio 17 2022" (runner has VS 2022 17.14 only; VS 18 is local-only) + §3/§6.3 aligned, (b) new §9.1 records the 4 user-locked decisions (Win32+D3D11 UI, 4K = post-1.0, first frame = Sprint 2, Apache-2.0) and §9.2 keeps transient-vs-PIN pairing as the remaining Sprint-1 decision, (c) gates 1.10 (first commit + ci.yml), 1.11 (pairing mode advertised consistently), 1.12 (persistent Ed25519 identity in Sprint 1) added, (d) §8 now states gate-order-not-calendar, (e) FFmpeg plan B pinned to BtbN **lgpl** build, vcpkg at C:/dev/vcpkg + binary cache/--clean-after-build for the 38 GB C: |
| 2026-09-16 | architect | prd-maker | docs/architecture/ARCHITECTURE-BRIEF.md | DONE — 10/10 sections, protocol direction corrected (PC = AirPlay receiver/advertiser, not RTSP client), UI = Win32+D3D11 for Sprint 1 (WPF not buildable: no .NET SDK), FFmpeg must come from vcpkg (installed gyan build is bin-only), Sprint 1 gate = discovery + GET /info only; pairing/FairPlay + first frame is the Sprint 2 gate |
| 2026-09-16 | prd-maker | ui-designer | docs/prd/PRD-AIRPLAY-SPRINT1.md | DONE — Feature Tree (F0–F3 + deferred), PRD, 6 Feature Specs, 11-task ordered Task List, gate checklist 1.1–1.9. Normative corrections folded in: §0.1 CI generator = VS17 2022/Ninja (brief §6.3 contradictory — @architect owes 1-line patch), §0.2 §9 open-decision items owed, §8.2 Ed25519 identity generated in Sprint 1 with advertise_pk default OFF (R14), §4.4 tasks framed as gate sequence not calendar, Task 0 = toolchain blocker (CMake/vcpkg/Ninja/FFmpeg-dev). 4 user decisions in §11. **NOT yet built**: pairing/FairPlay, RTP, decode, real video frame, WPF, installer. |
| 2026-09-16 | ui-designer | code-executor | docs/design/DESIGN.md **rev 2** + `assets/i18n/{en,id}.json` | DONE — rev 2 folds in **D6 (i18n, user decision: English default + multi-language)** and **D5 (pairing = transient)**. New §7 = i18n contract (files `assets/i18n/<locale>.json`, `locale` config key + `set_locale` on the bridge, **three-tier fallback: active locale → `en` → compiled-in English literal — never a blank, never a raw key id**, placeholders `{name}` reorderable, numbers formatted by the renderer not the locale file, one sentence = one key, no fragment concatenation). New §3.4 = **i18n layout tolerance** (measure text, card grows, never hardcode string pixel width — the classic i18n bug). **T7 never calls `i18n::get()`** — T9 resolves strings into `OverlayModel` (§4.2), so a locale switch can't stall a frame. §6 copy deck now keyed with en/id columns; §8 failure card + §13 wireframes in English default with **A + A′ (en + id) as the gate 1.7 evidence**; §11 adds the "localisation lives in the UI layer, never in the core" rule. **Copy artifacts authored by design:** `en.json` + `id.json` (62 keys each) — verified programmatically: key parity **0 drift**, **0 placeholder mismatch**, every state label ≤12 chars, **0 empty values**. §9.1 = transient (built path, 3 keys); §9.4 = PIN card **parked, not built** (was §8.2). Roller: @code-executor wires the loader + `locale` config key; the copy is mine. **Not claimed:** no runtime locale switch in Sprint 1 (config + restart only), no video frame, no decoder, no audio surface, no PIN card, no tray, no settings UI. **rev 2.1 (same day, review follow-up):** §5.1 amended after the implementation used GDI `TextOutW` — overlay may rasterise via **GDI → DIB section → D3D11 texture** instead of DirectWrite, **provided it composites into the flip-model swap chain** (GDI on the HWND/back buffer is forbidden: `IDXGISurface1::GetDC` is unsupported on flip-model, which brief §3 mandates); **grayscale AA is mandatory, ClearType forbidden** because the card is 78 % translucent. **Gate 1.6 wording unchanged** — it tests observable output, not a rasteriser. |
